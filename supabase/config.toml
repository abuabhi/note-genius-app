
# A string used to distinguish different Supabase projects on the same host. Defaults to the project ID.
project_id = "zuhcmwujzfddmafozubd"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. public and storage are always included.
schemas = ["public", "storage", "graphql_public"]
# Extra schemas to add to the search_path of every request. public is always included.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 15

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# The maximum number of connections the pooler will keep open with the database. The pooler_size
# value must be greater than max_clients.
pool_size = 20
# How many client connections are allowed. It must be greater than 0.
max_clients = 10
# The time spent waiting, in milliseconds, before changing the database connection.
default_pool_size = 15
# How many server connections to allow for each user/database pair.
pool_mode = "transaction"
# How many server connections to retain. It must be less than or equal to pool_size.
max_db_connections = 5

[realtime]
enabled = true
# Bind realtime via either IPv4 or IPv6. (default: IPv6)
ip_version = "IPv6"
# The maximum number of bytes per message (1 byte = 1 char for ASCII).
max_message_size = 1048576

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://localhost"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post-authentication.
additional_redirect_urls = ["https://localhost:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true

[analytics]
enabled = false
port = 54327
# Configure analytics for Supabase to track Postgres queries and performance.
vector_port = 54328

# Configure Postgres GraphQL server
[graphql]
enabled = true
# The maximum depth allowed for a GraphQL query.
max_depth = 8
graphiql = true

# Configure logflare server
[logflare]
enabled = false
port = 54326

# Configure functions server
[functions]
enabled = true
port = 54325
function_port = 9000

[functions.process-reminders]
verify_jwt = true
import_map = "supabase/functions/import_map.json"
entrypoint = "index.ts"

[functions.setup-reminder-cron]
verify_jwt = true
import_map = "supabase/functions/import_map.json"
entrypoint = "index.ts"
