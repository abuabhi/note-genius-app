
import { UnifiedFloatingTimer } from './UnifiedFloatingTimer';

// Legacy component - use UnifiedFloatingTimer instead
export const GlobalFloatingTimer = () => {
  return <UnifiedFloatingTimer />;
};
