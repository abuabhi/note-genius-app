import React from 'react';

// This component is now deprecated in favor of the sidebar SessionTimer
// Keeping it as a stub to avoid breaking imports
export const UnifiedSessionDock = () => {
  return null;
};
